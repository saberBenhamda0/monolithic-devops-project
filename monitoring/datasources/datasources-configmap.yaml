apiVersion: v1
kind: ConfigMap
metadata:
  name: production-datasource
  labels:
    grafana_datasource: "1"
data:
  production-datasource.yaml: |
    apiVersion: 1

    datasources:
      - name: Prometheustesttest
        type: prometheus
        uid: prometheus
        access: proxy
        url: http://prometheus-kube-prometheus-prometheus:9090
        isDefault: false
        version: 1
        editable: false
        jsonData:
          {}