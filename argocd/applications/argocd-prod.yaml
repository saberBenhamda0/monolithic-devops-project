apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/saberBenhamda0/monolithic-devops-project.git
    targetRevision: HEAD
    path: backend/overlays/prod
  destination:
    server: https://kubernetes.default.svc  # same cluster
    namespace: prod
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
